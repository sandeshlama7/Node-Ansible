plugin: amazon.aws.aws_ec2
region: "us-east-1"
filters:
  instance-state-name: running
  "tag:owner": sandesh.lama
  "tag:Name": EC2-Sandesh-Terraform-01
hostnames:
  - instance-id

# groups:
#   node: "'01' in tags.Name"
